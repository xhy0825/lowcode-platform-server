<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lowcode.platform.system.mapper.SysRoleMapper">

    <resultMap id="SysRoleResult" type="com.lowcode.platform.system.entity.SysRole">
        <id column="id" property="id"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="role_name" property="roleName"/>
        <result column="role_code" property="roleCode"/>
        <result column="data_scope" property="dataScope"/>
        <result column="status" property="status"/>
        <result column="del_flag" property="delFlag"/>
        <result column="created_by" property="createdBy"/>
        <result column="created_time" property="createdTime"/>
        <result column="updated_by" property="updatedBy"/>
        <result column="updated_time" property="updatedTime"/>
    </resultMap>

    <select id="selectRolesByUserId" resultMap="SysRoleResult">
        SELECT r.* FROM sys_role r
        INNER JOIN sys_user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId} AND r.del_flag = 0
    </select>

    <select id="selectPermissionIdsByRoleId" resultType="Long">
        SELECT permission_id FROM sys_role_permission WHERE role_id = #{roleId}
    </select>

</mapper>