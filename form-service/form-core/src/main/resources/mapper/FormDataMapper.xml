<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lowcode.platform.form.mapper.FormDataMapper">

    <select id="selectByFormDefinitionId" resultType="com.lowcode.platform.form.entity.FormData">
        SELECT * FROM form_data_template WHERE form_definition_id = #{formDefinitionId} AND del_flag = 0
        ORDER BY created_time DESC
        LIMIT #{pageSize} OFFSET #{pageNum * pageSize}
    </select>

</mapper>
